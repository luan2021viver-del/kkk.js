#include <stdio.h>
int main() {
   
   printf("todos os numeros pares entre 1 e 20");
for (int i = 0; i < 20; i++) {
    if (i % 2 == 0){
       printf("%d\n", i);
     }
    }

int numero;
printf("\n---TABUADA---\n");
printf("Digite um numero: ");
scanf("%d", &numero);
 for(int i = 1; i <= 10; i++){
int resultado = i * numero;
 printf("%d x %d = %d\n",numero,i,resultado);
 
 }
  return 0;
  
  
}
