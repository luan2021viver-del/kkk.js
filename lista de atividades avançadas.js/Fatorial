#include <stdio.h>

int main() {
    int Numero, i;
    long int fatorial = 1;

    printf("Digite um numero inteiro nao negativo: ");
    scanf("%d", &N);

    for(i = 1; i <= Numero; i++) {
        fatorial *= i;
    }

    printf("Fatorial de %d = %ld\n", Numero, fatorial);

    return 0;
}
