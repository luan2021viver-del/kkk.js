package com.mycompany.helloworld;

import java.util.Scanner;

public class HelloWorld {

    public static void main(String[] args) {

        Scanner tc = new Scanner(System.in);

        int A, B;
        int nascimento;
        int idade;
        int num;
        int antecessor;
        int sucessor;
        double media;

        // Primeiro e segundo número
        System.out.print("Qual o primeiro numero? ");
        A = tc.nextInt();

        System.out.print("Qual o segundo numero? ");
        B = tc.nextInt();

        // Verifica qual número é maior
        if (A > B) {
            System.out.println("A é o maior numero.");
        } else if (B > A) {
            System.out.println("B é o maior numero.");
        } else {
            System.out.println("Os números são iguais.");
        }

        // Calcula a média
        media = (A + B) / 2.0;
        System.out.println("A média entre eles é: " + media);

        // Calcula a idade
        System.out.print("Qual o seu ano de nascimento? ");
        nascimento = tc.nextInt();

        idade = 2026 - nascimento;
        System.out.println("A sua idade é: " + idade);

        // Antecessor e sucessor
        System.out.print("Escolha um numero: ");
        num = tc.nextInt();

        antecessor = num - 1;
        sucessor = num + 1;

        System.out.println("O antecessor de " + num + " é: " + antecessor);
        System.out.println("O sucessor de " + num + " é: " + sucessor);

        // Verifica se é par ou ímpar
        if (num % 2 == 0) {
            System.out.println("O número " + num + " é PAR.");
        } else {
            System.out.println("O número " + num + " é ÍMPAR.");
        }

        tc.close();
    }
}

Erro: Não foi possível encontrar ou carregar a classe principal HelloWorld. Causado por: java.lang.NoClassDefFoundError: HelloWorld (nome incorreto: com/mycompany/helloworld/HelloWorld)
